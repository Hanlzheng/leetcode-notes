# 通用知识点 / 语法模式

## range 的用法
- `range(n)` → 0, 1, ..., n-1
- `range(a, b)` → a, a+1, ..., b-1（含头不含尾）
- 写之前问自己两句：第一个数字是几？结尾 = 最后一个下标 + 1
- 遍历「i 之后的所有下标」→ `range(i+1, len(nums))`
- `range(len(nums))` 本身不会越界，不需要额外 -1

## list.index(值, 起始位置)
- 第二个参数指定从哪个下标开始找
- 数组有重复值时，不指定起点会永远返回第一个匹配 → 容易出坑
- 教训：要返回下标的题，别「找到值再反查下标」

## 值 vs 下标
- 只关心元素是什么 → `for x in nums:`（x 是值）
- 需要知道位置（返回下标、比较位置、nums[j] 索引）→ `for i in range(len(nums)):`（i 是下标）

## 三元表达式
- 值A if condition else 值B